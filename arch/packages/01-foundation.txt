# Fundacao do sistema (Fase 0) — Arch + Hyprland
# Instalado por: arch/install/01-foundation.sh
# Linhas com # e linhas em branco sao ignoradas.

# --- GPU: NVIDIA (Optimus) + Intel ---
nvidia-open-dkms      # driver aberto p/ Ampere+ (RTX 3050). dkms = rebuild automatico
nvidia-utils
lib32-nvidia-utils    # libs 32-bit (Steam/Wine)
nvidia-prime          # fornece `prime-run` p/ render offload
egl-wayland
linux-headers         # necessario p/ dkms compilar
dkms

# --- Sessao grafica: portais e autenticacao ---
hyprpolkitagent           # agente polkit (pede senha p/ montar discos, etc.)
xdg-desktop-portal-hyprland   # screenshare, file pickers (backend Hyprland)
xdg-desktop-portal-gtk

# --- Audio: PipeWire ---
pipewire
pipewire-pulse
pipewire-alsa
pipewire-jack
wireplumber

# --- Rede ---
networkmanager        # instalado aqui; a TROCA do iwd ocorre na Fase 2

# --- Terminal & fontes base ---
kitty
ttf-jetbrains-mono-nerd
noto-fonts
noto-fonts-emoji
noto-fonts-cjk
