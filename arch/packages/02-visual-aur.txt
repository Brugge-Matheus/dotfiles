# Fase 2 — Camada visual (AUR, via yay) — Arch + Hyprland
# Instalado com: yay -S --needed <pacotes>
# (ou automaticamente por arch/install.sh, se o yay estiver presente)

rofi-file-browser-extended   # extensao do rofi: navegar/abrir arquivos (SUPER+SHIFT+E)
qimgv                        # visualizador de imagens Qt6 (rapido) + edicao (crop/rotate/flip/resize) e webm

# Nota: o launcher e o rofi (repo OFICIAL — ver 02-visual.txt), Wayland nativo.
# O antigo walker + elephant foi aposentado: iniciava frio/lento e dependia de
# um daemon (elephant) + servico residente do frontend. rofi 2.0 abre
# instantaneo, sem daemon.
