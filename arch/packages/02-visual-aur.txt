# Fase 2 — Camada visual (AUR, via yay) — Arch + Hyprland
# Instalado com: yay -S --needed <pacotes>
# (ou automaticamente por arch/install.sh, se o yay estiver presente)

walker                          # launcher (frontend, estilo omarchy)

# Backend do walker (elephant) + provedores. Variantes -bin = pre-compiladas.
elephant-bin                    # core (daemon de dados)
elephant-desktopapplications-bin # busca/abertura de apps (essencial p/ o launcher)
elephant-calc-bin               # calculadora inline
elephant-runner-bin             # rodar comandos
elephant-files-bin              # busca de arquivos
elephant-menus-bin              # menus customizados (ex.: power menu)
elephant-clipboard-bin          # historico de clipboard
